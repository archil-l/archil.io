import React from 'react';

type HeaderProps = {
    navLinks: {
        title: string,
        href: string,
    }[]
}

const Header = ({ navLinks }: HeaderProps) => {
    return null;
}

export default Header;