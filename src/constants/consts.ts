export enum sectionIds {
  Welcome = 'welcome',
  Work = 'work',
  Projects = 'projects',
  Contact = 'contact',
  Teaching = 'teaching',
  Agent = 'agent',
}

export enum PageIds {
  Home = 'home',
  Teaching = 'teaching',
  Agent = 'agent',
}
